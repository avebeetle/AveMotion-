# Part26F own ellipse admission execution ledger

Spec: docs/superpowers/specs/2026-09-24-own-ellipse-admission-design.md
Plan: docs/superpowers/plans/2026-09-24-own-ellipse-admission.md
Stage BASE7000dd0b7470a17fd21ad3515a44eb7f140adae9.
SDD workspace: .superpowers/sdd/2026-09-24-own-ellipse-admission/.

## Authority and prerequisite

User requests long continuation toward own Lottie/TGS engine statically embedded
in Avelabs-UI, delegates written spec/plan/SDD and reversible main commits/ordinary
push. Controller approved spec009dd2e and the detailed plan after self-review;
not a claim of user review of unseen artifacts. No automation resumed.

E complete at tested9212c03, handoff7000dd0 ordinary-pushed, exact remote equality/
clean tree verified. Fresh none32/32, Telegram76/76, preview70/70; Samsung configure,
vendor/TGS16, actual graph and syntax-only header traces98/155 pass. No E product
task repeated. Existing direct-main checkout (.git==common-dir) honors user choice.

## Rulings

1. Ruling: one compiled borrowed-table core, not duplicated grammar or templated
   parser-like cursors — preserves one source of exact arithmetic/schema truth and
   keeps vendor types outside core — cost if wrong: bounded extra projection/table
   storage, with no performance or allocation benefit claimed.
2. Ruling: keep legacy parser/resources/raw-event failures and diagnostic quirks,
   separately name own-document admission with E's policies — no silent route/API
   replacement — cost if wrong: two explicit compatibility contracts remain until
   a later production-loader decision; common-core equality is not an independent
   grammar oracle.
3. Ruling: document-only own entry in F, caller composes E errors explicitly — no
   unused byte/audit facade or fallback — cost if wrong: actual loader integration
   later needs a small composition seam; this stage is not whole animation loading.
4. Ruling: retain raw SAX strings/DOM through synchronous core and normalize only
   after complete alignment — needed for borrowed lifetimes and one normalization
   implementation — cost if wrong: extra retained events and different internal
   allocation timing; externally visible parse/capture/schema failures must remain.
5. Ruling: direct main/scoped pushes and raw/SDD retention per user instruction,
   no worktree cleanup or rewrite — cost if wrong: less isolation/retained scratch;
   one writer, protected scopes, review and remote guard provide safeguards.

## Preflight

| Producer/consumer | Cross-check | Resolution |
| --- | --- | --- |
| Task1 core / Task2 own adapter | NativeEllipseValue kind/key/scalar/links, uint32 sentinel/root0, span/core output pointer | Exact names/signatures in plan; rows retained in original order; immediate sibling links, not ordinal arithmetic. |
| Legacy frontend / shared core | DOM/event owner outlives borrowed views; raw parse/kind/count failures remain InvalidJson `/` | Written explicit owner and normalization factoring; no dangling local handler views or new parser policy. |
| Task1 helper / both tasks | Independently literal full NativeEllipseInput factory, source projection is test setup only | Exact all-field baseline in plan; no product normalization for expected values. |
| Both / CMake | Unconditional core and own adapter, legacy Telegram-only, own/core tests all variants, compatibility/differential Telegram-only | One sequential writer; Runtime intentionally reference-linked in Telegram, none closure separately verified. |
| Both / error policies | Shared schema but E number/Unicode/empty-ancestor differences | Literal own/old expectations; not a generic equality gate or skipped mismatch. |
| Self / scope | New private APIs versus unchanged descriptor/old entry headers/old tests/Runtime route/UI | Exact file map and protected boundaries; no vendor/license/dependency mutation. |

Main agent read full legacy implementation, E interface/spec and independent draft
audit; raw owner, noncontiguous child and factoring clarifications incorporated.
Self-review: spec clauses map to Task1 extraction/legacy, Task2 own/lifetime/policies,
root platform/boundaries. All five Review Focus items have explicit tests. No plan
placeholder or type-name mismatch found. Core stub functional RED is a new entry
test, not an artificial regression of old behavior; old baseline GREEN stays labeled.

## Tasks

- [ ] Task1 shared core and preserved legacy frontend. Worker BASE is final preflight HEAD, recorded before dispatch.
- [ ] Task2 own adapter and independent/all-variant evidence, only after Task1 review.
- [ ] Root final platform/provenance/include/link/install gates and independent whole-stage review.
- [ ] Next own-model/resources/certificate design, not F own-playback completion.

No active writer/build at preflight. Root owns STATE/ledger/docs; workers own only
their scoped product/test files and reports. Retain every RED/failure and use fresh
stable source identities. No UI or automation actions.
